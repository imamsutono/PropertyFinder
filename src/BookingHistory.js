import React, { Component } from "react";
import { Text } from "react-native";

export default class BookingHistory extends Component {
  render() {
    return <Text>Ini riwayat pemesanan</Text>;
  }
}
